### heroku setting
Procfile
runtime.txt

```bash
brew install heroku/brew/heroku
```

### git project
```bash
git init
git add .
git commit -m "commit"
git remote add origin ""
git push -u origin main
```

### HEROKU 가입